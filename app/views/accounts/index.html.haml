%h1= t("accounts_index_heading")

= link_to t("accounts_index_new"), new_account_path, class: "btn btn-primary"

- @accounts.each do |account|
  %p
    = link_to account.name, account
    = link_to "delete", account, method: :delete, data: { confirm: "weet je het zeker?"}