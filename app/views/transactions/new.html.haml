%h1= t('transactions_new_heading')

= simple_form_for [@account, @transaction] do |f|
  = f.input :transaction_type, collection: ["debit", "credit"]
  = f.input :description
  = f.input :amount
  = f.input :fixed_transaction
  = f.submit
